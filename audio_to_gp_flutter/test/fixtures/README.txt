Place `coronaria-che-esplode.mp3` here before running the E2E integration tests.

The file is not included in the repository due to copyright. Copy it from the
project's `input/` directory (or download the song yourself).

Required by: test/integration/pipeline_e2e_test.dart
